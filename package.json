{
  "name": "tuition-app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.20.0"
  }
}
